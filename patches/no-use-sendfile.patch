diff -uNr samba-3.0.7.orig/docs/htmldocs/smb.conf.5.html samba-3.0.7/docs/htmldocs/smb.conf.5.html
--- samba-3.0.7.orig/docs/htmldocs/smb.conf.5.html	2004-09-11 09:14:38.000000000 -0700
+++ samba-3.0.7/docs/htmldocs/smb.conf.5.html	2004-10-26 12:52:00.000000000 -0700
@@ -3727,7 +3727,7 @@
     and cause Samba to be faster. Samba automatically turns this off for clients
     that use protocol levels lower than NT LM 0.12 and when it detects a client is
     Windows 9x (using sendfile from Linux will cause these clients to fail).
-    </p><p>Default: <span class="emphasis"><em><i class="parameter"><tt>use sendfile</tt></i> = yes
+    </p><p>Default: <span class="emphasis"><em><i class="parameter"><tt>use sendfile</tt></i> = no
 </em></span>
 </p></dd><dt><span class="term"><a name="USESPNEGO"></a>use spnego (G)</span></dt><dd><p>This variable controls controls whether samba will try 
     to use Simple and Protected NEGOciation (as specified by rfc2478) with 
diff -uNr samba-3.0.7.orig/docs/manpages/smb.conf.5 samba-3.0.7/docs/manpages/smb.conf.5
--- samba-3.0.7.orig/docs/manpages/smb.conf.5	2004-09-11 09:13:03.000000000 -0700
+++ samba-3.0.7/docs/manpages/smb.conf.5	2004-10-26 12:52:10.000000000 -0700
@@ -3985,7 +3985,7 @@
 use sendfile (S)
 If this parameter is \fByes\fR, and the \fBsendfile()\fR system call is supported by the underlying operating system, then some SMB read calls (mainly ReadAndX and ReadRaw) will use the more efficient sendfile system call for files that are exclusively oplocked\&. This may make more efficient use of the system CPU's and cause Samba to be faster\&. Samba automatically turns this off for clients that use protocol levels lower than NT LM 0\&.12 and when it detects a client is Windows 9x (using sendfile from Linux will cause these clients to fail)\&.
 
-Default: \fB\fIuse sendfile\fR = yes \fR 
+Default: \fB\fIuse sendfile\fR = no \fR 
 
 .TP
 use spnego (G)
diff -uNr samba-3.0.7.orig/source/param/loadparm.c samba-3.0.7/source/param/loadparm.c
--- samba-3.0.7.orig/source/param/loadparm.c	2004-10-26 12:44:49.000000000 -0700
+++ samba-3.0.7/source/param/loadparm.c	2004-10-26 12:41:19.000000000 -0700
@@ -541,7 +541,7 @@
 	False,			/* bDefaultDevmode */
 	True,			/* bNTAclSupport */
 	False,                  /* bForceUnknownAclUser */
-	True,			/* bUseSendfile */
+	False,			/* bUseSendfile */
 	False,			/* bProfileAcls */
 	False,			/* bMap_acl_inherit */
 	False,			/* bAfs_Share */
