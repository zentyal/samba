--- samba-2.999+3.0.alpha20.orig/source/utils/status.c	Thu Sep 26 21:38:36 2002
+++ samba-2.999+3.0.alpha20/source/utils/status.c	Thu Oct 10 19:14:49 2002
@@ -646,6 +646,16 @@
 	if (!shares_only) {
 		int ret;
 
+		tdb = tdb_open_log(lock_path("locking.tdb"), 0, TDB_DEFAULT, O_RDONLY, 0);
+
+		if (!tdb) {
+			d_printf("%s not initialised\n", lock_path("locking.tdb"));
+			d_printf("This is normal if an SMB client has never connected to your server.\n");
+			exit(0);
+		} else {
+			tdb_close(tdb);
+		}
+
 		if (!locking_init(1)) {
 			d_printf("Can't initialise locking module - exiting\n");
 			exit(1);
