# Defaults for samba initscript
# sourced by /etc/init.d/samba
#
# To change values in here run "dpkg-reconfigure samba". Do not change
#	manually because values will change next time the package is
#	upgraded.

#
# This is a POSIX shell fragment
#

# How should Samba (nmbd and smbd) run? Possible values are "daemons"
#	or "inetd".
RUN_MODE=""
